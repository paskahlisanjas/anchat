<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/profile_picture"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:layout_margin="@dimen/chat_list_item_margin"
        android:layout_alignParentStart="true"
        android:layout_alignParentLeft="true" />
    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/profile_picture"
        android:text="nama"
        android:textStyle="bold"
        android:textSize="24sp"
        android:singleLine="true"
        android:layout_marginTop="@dimen/chat_list_item_margin"/>
    <TextView
        android:id="@+id/status_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:text="ini status"
        android:layout_alignParentStart="@id/name"
        android:layout_below="@id/name"
        android:layout_toRightOf="@id/profile_picture"
        android:layout_toLeftOf="@id/timestamp"
        android:layout_alignParentLeft="@id/name"
        android:layout_marginBottom="@dimen/chat_list_item_margin" />

</RelativeLayout>